{% extends '@admin_views/layout.html.twig' %}

{% block title %}{{ name_admin() }} | Admin Roles - Edit {{ cms_role.name }}{% endblock %}

{% block body %}
    <div class="content-wrapper">
        <section class="content-header">
            <h1>
                Admin
                <small>Role <b>{{ cms_role.name }}</b></small>
            </h1>
            <ol class="breadcrumb">
                <li><i class="fa fa-shield"></i> Admin</li>
                <li><a href="{{ path('admin_cms_roles_index') }}"><i class="fa fa-unlock-alt"></i> Roles</a></li>
                <li class="active">{{ cms_role.name }}</li>
            </ol>
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="box box-warning">
                <div class="box-header with-border">
                    {{ include('@admin_views/admin/_cms_roles/_form.html.twig', {'button_label': 'update', 'button_class': 'success bg-olive', 'i_class': 'fa fa-edit'}) }}
                    <a href="{{ path('admin_cms_blocks_index') }}" class="btn btn-default pull-right"><i class="fa fa-chevron-left"></i> back to list</a>
                    {{ include('@admin_views/admin/_cms_roles/_delete_form.html.twig') }}
                </div>
            </div>
        </section>
        <!-- /.content -->
    </div>
{% endblock %}
